;(function( window, $, undefined ) {

"use strict";

// 非 IE8 以下浏览器才有功能
if ( $ && (!$.browser.msie || $.browser.version > 7) ) {
  $.fn.tooltip = function() {
    
  };
}
  
})( window, window.jQuery );
